# This file includes global variables that will be available inside your project
# 1. In the front end code you can access this variables like this: console.log(import.meta.env.VARIABLE_NAME)
# 1. In the back end code you access the variable by importing os and then typing print(os.getenv('VARIABLE_NAME'))

# Back-End Variables
FLASK_APP_KEY="any key works"
JWT_SECRET_KEY="super-secret-key-change-in-production"
FLASK_APP=src/app.py
FLASK_DEBUG=1
DEBUG=TRUE

# =============================================================================
# DATABASE_URL - Configuracion de base de datos
# =============================================================================
# Usa UNA de las siguientes opciones segun tu entorno:
#
# --- OPCION 1: Microsoft SQL Server (local Windows) ---
# DATABASE_URL=mssql+pyodbc://sa:TuPassword@localhost/NombreDB?driver=ODBC+Driver+17+for+SQL+Server
#
# --- OPCION 2: PostgreSQL (Codespaces / Gitpod / produccion) ---
# DATABASE_URL=postgres://gitpod:postgres@localhost:5432/example
#
# --- OPCION 3: SQLite (fallback si no se define DATABASE_URL) ---
# (simplemente no definas DATABASE_URL y usara SQLite automaticamente)
# =============================================================================

DATABASE_URL=postgres://gitpod:postgres@localhost:5432/example

# Front-End Variables
VITE_BASENAME=/
#VITE_BACKEND_URL=
